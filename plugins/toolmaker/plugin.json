{
  "name": "toolmaker",
  "version": "1.1.0",
  "description": "Tool Maker — create, read, and activate custom tools",
  "author": "sapphire",
  "url": "https://sapphireblue.dev",
  "priority": 50,
  "capabilities": {
    "tools": ["tools/toolmaker.py"],
    "settings": [
      {
        "key": "validation",
        "type": "string",
        "label": "Validation Level",
        "default": "strict",
        "widget": "select",
        "options": [
          {"label": "Strict — safe imports only (recommended)", "value": "strict"},
          {"label": "Moderate — blocks dangerous ops, any import", "value": "moderate"},
          {"label": "Trust — syntax check only (dangerous)", "value": "trust"}
        ],
        "help": "How strictly to validate AI-written tool code. Strict allows 40+ safe modules including requests, json, datetime — covers most tools.",
        "confirm": {
          "values": ["moderate", "trust"],
          "title": "Reduced Validation",
          "warnings": [
            "Moderate allows any import not explicitly blocked",
            "Trust mode only checks for syntax errors",
            "Both modes allow imports that could access system resources",
            "Only use this if you fully trust your LLM and prompt setup"
          ],
          "buttonLabel": "I understand the risks"
        }
      }
    ]
  }
}
